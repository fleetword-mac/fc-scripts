# FC API Platform Template

Starter deployment files for the `fc-api-platform` stack.

Use these when spinning up the platform stack on a new VPS:

- `docker-compose.yml`
- `.env.example`

This template is intentionally generic enough to copy into a fresh app repo or VPS working directory, while still matching the current Final Concept licensing platform layout.
